id: hotpot_teacher_guided_b1_plan_review
name: "HotpotQA Teacher-Guided Agent, B=1 + plan review"
description: "Optional preflight plan review, then a single forced-finish action step."
reasoning_style: teacher_guided

components:
  - type: teacher_guided_plan_review
    config: {enabled: true}
  - type: teacher_guided_agent_step
    config: {step_index: 1, budget: 1, force_finish: true}

config:
  max_steps: 2  # plan review + 1 action step
