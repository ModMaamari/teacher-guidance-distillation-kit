# E16 — Choosing the judge

Grade a labelled set of answers with every candidate judge and compare accuracy, kappa and error types.

Status and result: `experiments/REGISTRY.md`, `results/E16_judge_selection/README.md`.

```bash
# the labelled items, every call and the scoring script are in results/E16_judge_selection/
python results/E16_judge_selection/score.py results/E16_judge_selection
```
