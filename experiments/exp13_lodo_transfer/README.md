# E13 — Leave-one-dataset-out transfer

Train on three datasets, evaluate on the unseen fourth; four folds.

Status and result: `experiments/REGISTRY.md`, `results/E13_lodo_transfer/README.md`.

```bash
# docs/REPRODUCE.md section 3 (group A): train data/splits/lodo/fold_<ds>, evaluate on full_<ds> and the held-out sets
```
